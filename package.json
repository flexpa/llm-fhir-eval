{
  "name": "@flexpa/llm-fhir-eval",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "eval": "bunx promptfoo eval",
    "build": "bun run dev && bunx promptfoo share"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
